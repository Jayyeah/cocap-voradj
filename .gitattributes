# Immutable upstream snapshots retain byte-for-byte source, including the
# donors' original whitespace. Suppress textual diffs so project-wide
# diff --check applies to local implementation files without rewriting donors.
vendor/upstream/** -diff
