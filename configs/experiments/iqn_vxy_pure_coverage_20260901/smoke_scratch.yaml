extends: scratch.yaml

output_root: /tmp/cocap_vxy_pure_coverage_smoke_scratch
run_name: scratch
total_timesteps: 2

iqn:
  checkpoint_freq: 1

checkpointing:
  full_resume: true
  full_resume_path: /tmp/cocap_vxy_pure_coverage_smoke_scratch/resume_latest.pt

experiment_metadata:
  smoke_only: true

