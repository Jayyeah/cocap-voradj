extends: warm_start.yaml

output_root: /tmp/cocap_vxy_pure_coverage_smoke_warm
run_name: warm
total_timesteps: 2

iqn:
  checkpoint_freq: 1

checkpointing:
  full_resume: true
  full_resume_path: /tmp/cocap_vxy_pure_coverage_smoke_warm/resume_latest.pt

experiment_metadata:
  smoke_only: true

