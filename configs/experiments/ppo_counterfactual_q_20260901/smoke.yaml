extends: common.yaml

seed: 2026090199
run_name: ppo_cf_smoke_20260901

small_step_ac:
  total_env_steps: 4
  checkpoint_interval: 2
  eval_episodes: 1
  log_interval: 1
  run_dir: artifacts/2026-09-01_ppo_counterfactual_q/smoke
  retain_final_full_resume: true
  mappo:
    rollout_length: 2
    ppo_epochs: 1
    minibatches: 1

experiment_metadata:
  seed_index: 0
  purpose: finite update, milestone, dual eval and exact 2-to-4 full-resume smoke only
