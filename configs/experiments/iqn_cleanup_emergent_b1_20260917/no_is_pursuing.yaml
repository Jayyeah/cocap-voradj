extends: ../forward_final_mappo_20260908/stage1_4v1.yaml

# B1 policy-only ablation.  The environment still computes effective role
# state for reward, replay metadata, and evaluator diagnostics; this switch
# only controls the policy observation view.
perception:
  self_feature_dim: 8
  include_is_pursuing: false

iqn:
  self_feature_dim: 8
  pursuer_feature_dim: 6
  pursuing_embed_dim: 0
  include_is_pursuing: false

experiment_metadata:
  series_label: iqn_cleanup_emergent_b1_no_is_pursuing_20260917
  scope: policy-observation-only; no reward, topology, z_i, or scratch-RL change
  teacher_checkpoint: artifacts/2026-08-04_crms_vctls_ce_final/checkpoints/stage1_4v1_step_2000000.pt
  teacher_checkpoint_sha256: 2f39ea046acb56321585e7bbe1a8f0869a4dd85777ff2ae65ca29a7bd5680c89
  removed_policy_inputs:
    - self.is_pursuing
    - friend.is_pursuing
    - role_dependent_friend_ordering
