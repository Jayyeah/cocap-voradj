extends: z05_stage2_8p2e2obs_700k.yaml

seed: 2026091903
run_name: stage3_training

reward:
  coverage_ce_min_active_pursuers: 12
  min_active_pursuers: 12
  post_capture_coverage_window_steps: 700

tasks:
  voradj:
    env:
      num_pursuers: 12
      num_evaders: 3
      num_obstacles: 3
      pursuer_spawn_mode: map_random
      evader_spawn_mode: map_random
      pursuer_spawn_min_sep: 15.0
      evader_spawn_min_sep: 15.0
  voradj_coverage:
    env:
      num_pursuers: 12
      num_evaders: 0
      num_obstacles: 3
      pursuer_spawn_mode: inner_random_cluster
      spawn_cluster_radius: 13.0
      pursuer_spawn_min_sep: 7.0

experiment_metadata:
  stage: stage3_12p3e3obs
  stage_timesteps: 700000
  training_start: warm_start_from_own_stage2_selected_model_only

