extends: ../cr_ms_support_approach_ce_curriculum_20260802/stage1_4p1e1obs_scratch2m.yaml

# Only environment-contract change from canonical Historical Final.
# Algorithm/runtime metadata belongs in the runner manifest, not this overlay.
env:
  collision_semantics: synchronized_swept_v1
